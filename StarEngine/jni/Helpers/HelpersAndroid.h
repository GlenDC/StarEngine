#pragma once

#ifdef ANDROID
/*
//[TODO] Implent new android code (2.0)
#include "../defines.h"
#include "SerializedData.h"

namespace star_a
{
	void ReadFileAsset(const tstring & path, star::SerializedData & data);
	bool ReadFileAssetSafe(const tstring & path, star::SerializedData & data, bool logWarning = true);
}
*/
#endif
